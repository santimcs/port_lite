<%= form_for(latest_date) do |f| %>
  <% if latest_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(latest_date.errors.count, "error") %> prohibited this latest_date from being saved:</h2>

      <ul>
      <% latest_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
